# v3.13.7 — Footer Stability Final

The four pillar pages now explicitly load the same footer-specific stylesheet
used by the stable Courses experience:

- `/learn/`
- `/think/`
- `/apply/`
- `/listen/`

The existing footer HTML was retained. No footer content was rewritten.

A small scoped stability layer prevents the pillar-page card/grid CSS from
affecting the footer's grid and bottom-row layout.

No Knowledge Graph, entity, search, course or content data was changed.
