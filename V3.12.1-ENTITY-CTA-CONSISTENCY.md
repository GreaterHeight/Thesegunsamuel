# v3.12.1 — Entity CTA Consistency Fix

Standardizes canonical entity destination CTAs from `Explore →` to `OPEN →`.

No entity data, relationships, routes, search behavior, or existing page layouts were changed.
