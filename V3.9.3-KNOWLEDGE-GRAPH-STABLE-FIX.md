# v3.9.3 — Knowledge Graph Stable Fix

This build fixes the regression observed after v3.9.2.

## Fixes
- Preserves the working 62-node graph dataset.
- Normalises `entities` into renderable nodes if `nodes` is unavailable.
- Uses explicit deployment-safe `/Thesegunsamuel/...` URLs.
- Preserves `VIEW CONNECTIONS` for nodes without public routes.
- Preserves `OPEN →` for nodes with canonical routes.
- Adds safe HTML escaping.
- Uses explicit graph-data loading failure feedback.
- Keeps the existing Knowledge Graph UI and site shell unchanged.
