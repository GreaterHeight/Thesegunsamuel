let A=[];const esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));function render(q=""){let z=A.filter(a=>(a.title+" "+a.category+" "+a.excerpt+" "+a.tags.join(" ")).toLowerCase().includes(q.toLowerCase()));document.querySelector("#article-count").textContent=`${z.length} insight${z.length===1?"":"s"}`;document.querySelector("#article-grid").innerHTML=z.map(a=>`<article class="card course-card"><div><div class="eyebrow">${esc(a.category)}</div><h3>${esc(a.title)}</h3><p>${esc(a.excerpt)}</p></div><a class="btn btn-primary" href="${a.id}/">Read Insight</a></article>`).join("")||'<div class="card"><h3>No insight found.</h3><p>Try another search.</p></div>'}async function init(){A=await fetch("../assets/editorial-index.json").then(r=>r.json());document.querySelector("#article-search").oninput=e=>render(e.target.value);render()}document.addEventListener("DOMContentLoaded",init);