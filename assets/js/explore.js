let ITEMS=[];
const esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
function render(q="",type="All"){const box=document.querySelector("#search-results"),count=document.querySelector("#result-count");let z=ITEMS.filter(x=>(type==="All"||x.type===type)&&(!q||(x.title+" "+x.type+" "+x.text+" "+(x.tags||[]).join(" ")).toLowerCase().includes(q.toLowerCase())));count.textContent=`${z.length} result${z.length===1?"":"s"}`;box.innerHTML=z.length?z.map(x=>`<article class="card course-card"><div><div class="eyebrow">${esc(x.type)}</div><h3>${esc(x.title)}</h3><p>${esc(x.text||"Explore this part of the SegunSamuel knowledge ecosystem.")}</p></div><div class="course-actions"><a class="btn btn-primary" href="../${x.url}">Explore</a></div></article>`).join(""):'<div class="card"><h3>No matching knowledge found.</h3><p>Try another phrase such as “strategy”, “patterns”, “leadership”, “AI” or “systems”.</p></div>';}
async function init(){try{ITEMS=await fetch("../assets/knowledge-index.json").then(r=>r.json())}catch(e){ITEMS=[]}let types=["All",...new Set(ITEMS.map(x=>x.type).filter(Boolean))];document.querySelector("#filters").innerHTML=types.map(t=>`<button class="btn btn-secondary" data-type="${t}">${t}</button>`).join("");let active="All";document.querySelector("#filters").onclick=e=>{let b=e.target.closest("[data-type]");if(!b)return;active=b.dataset.type;render(document.querySelector("#global-search").value,active)};document.querySelector("#global-search").oninput=e=>render(e.target.value,active);render();}
document.addEventListener("DOMContentLoaded",init);