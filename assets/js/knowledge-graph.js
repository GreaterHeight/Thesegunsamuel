let N=[],active="All";
const esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
function render(){let q=document.querySelector("#graph-search").value.toLowerCase();let z=N.filter(n=>(active==="All"||n.type===active)&&(!q||(n.title+" "+n.type+" "+n.text+" "+(n.tags||[]).join(" ")).toLowerCase().includes(q)));document.querySelector("#graph-count").textContent=`${z.length} nodes`;document.querySelector("#graph-results").innerHTML=z.map((n,i)=>`<article class="card course-card"><div><div class="eyebrow">${esc(n.type)}</div><h3>${esc(n.title)}</h3><p>${esc(n.text||"Knowledge node in the SegunSamuel ecosystem.")}</p>${(n.connections||[]).length?`<div class="eyebrow">Connected</div><ul>${n.connections.map(c=>`<li>${esc(c.title)} <small>— ${esc(c.relationship)}</small></li>`).join("")}</ul>`:""}</div>${n.url?`<a class="btn btn-primary" href="../${n.url}">Open Node</a>`:""}</article>`).join("")||'<div class="card"><h3>No nodes found.</h3><p>Try another search or choose All layers.</p></div>'}
function init(){N=[];fetch("../assets/knowledge-index.json").then(r=>r.json()).then(d=>{N=d;let types=["All",...new Set(N.map(x=>x.type).filter(Boolean))];document.querySelector("#layer-filters").innerHTML=types.map(t=>`<button class="btn btn-secondary" data-layer="${esc(t)}" style="margin:4px 0;width:100%">${esc(t)}</button>`).join("");document.querySelector("#layer-filters").onclick=e=>{let b=e.target.closest("[data-layer]");if(!b)return;active=b.dataset.layer;render()};document.querySelector("#graph-search").oninput=render;render()})}
document.addEventListener("DOMContentLoaded",init);